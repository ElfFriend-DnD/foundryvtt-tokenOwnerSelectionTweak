export const preloadTemplates = async function() {
	const templatePaths = [
		// Add paths to "modules/foundryvtt-tokenOwnerSelectionTweak/templates"
	];

	return loadTemplates(templatePaths);
}
